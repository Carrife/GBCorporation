export enum Errors {
    BadRequest = 400,
    InternalError = 500,
    SameLoginExists = 1,
    HiringExists = 2,
}

export enum ErrorTitles {
    ERROR = "Error",
    WARNING = "Warning",
    SUCCESS = "Success",
    INFO = "Info", 
}